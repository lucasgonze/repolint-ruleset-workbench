{
  "$schema": "./schema.json",
  "version": 2,
  "axioms": {},
  "rules": {
    "workflow-file-exists" : {
      "level": "error",
      "rule": {
        "type": "file-existence",
        "options": {
          "globsAny": [".github/workflows/nop.yml"]
        }
      }
    }
  }
}
